import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  border: "2px solid rgb(225, 155, 153)",
  borderRadius: "8px",
  display: "flex",
  marginTop: "40px",
  marginBottom: "40px",
}));
